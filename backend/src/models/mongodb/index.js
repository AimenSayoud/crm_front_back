const User = require('./user_model');
const CandidateProfile = require('./candidate.model');
const Company = require('./company.model');
const Skill = require('./skill.model');
const Job = require('./job.model');

module.exports = {
    User,
    CandidateProfile,
    Company,
    Skill,
    Job
};